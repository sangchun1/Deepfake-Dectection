name: resnet18
family: cnn
source: torchvision

backbone:
  name: resnet18
  pretrained: true
  in_channels: 3
  freeze: false

head:
  num_classes: 2
  dropout: 0.0

pooling: avg

checkpoint:
  path: null
  strict: true